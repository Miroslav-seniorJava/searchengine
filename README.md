# SearchEngine — Итоговый проект Skillbox (Java Developer)

## 📌 Описание проекта

SearchEngine — это учебный поисковый движок, который:
- обходит сайт (многопоточно),
- скачивает HTML,
- извлекает текст,
- лемматизирует содержание,
- сохраняет страницы, леммы и индексы в БД,
- выполняет поиск по релевантности,
- формирует сниппеты,
- отдаёт результаты через REST API.

Проект полностью соответствует техническому заданию Skillbox.

---

## 🛠 Стек технологий

### Backend:
- **Java 17**
- **Spring Boot**
- **Spring Web**
- **Spring Data JPA / Hibernate**
- **Lombok**
- **Jsoup**
- **H2 / MySQL**

### Инструменты:
- **Maven**
- **Git / GitHub**
- **IntelliJ IDEA**

---

## 🚀 Инструкция по запуску локально

### 1. Установить JDK 17
Проверка:
```
java -version
```

### 2. Убедиться, что установлен Maven
Проверка:
```
mvn -v
```

### 3. Клонировать проект
```
git clone https://github.com/USERNAME/searchengine.git
```
Заменить USERNAME на свой GitHub логин.

### 4. Перейти в папку проекта
```
cd searchengine
```

### 5. Собрать проект
```
mvn clean install
```

### 6. Запустить приложение
```
mvn spring-boot:run
```

После запуска сервер доступен по адресу:

```
http://localhost:8080/
```

---

## 📡 API проекта

#### ▶ Начать индексацию
`POST /api/startIndexing`

#### ▶ Остановить индексацию
`POST /api/stopIndexing`

#### ▶ Индексировать отдельную страницу
`POST /api/indexPage?url=URL`

#### ▶ Получить статистику
`GET /api/statistics`

#### ▶ Поиск
`GET /api/search?query=текст&site=адрес&offset=0&limit=20`

---

## 📂 Структура проекта

```
src/
 ├── main/
 │   ├── java/searchengine/
 │   │   ├── config/        # Список сайтов
 │   │   ├── controllers/   # API
 │   │   ├── dto/           # DTO объектов
 │   │   ├── model/         # JPA сущности
 │   │   ├── repository/    # Репозитории
 │   │   ├── services/      # Логика приложения
 │   │   └── SearchEngineApplication.java
 │   └── resources/
 │       ├── application.yml
 │       └── schema.sql
 └── test/
```

---

## 👤 Автор проекта
**Мирослав Самоваров**  
Java Developer (Skillbox)  
GitHub: https://github.com/USERNAME

---

## ✔ Статус проекта
Готово к итоговой сдаче и демонстрации работодателям.
